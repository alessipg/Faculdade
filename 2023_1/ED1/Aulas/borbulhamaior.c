/*analise de complexidade do algoritmo de ordenação por seleção
a) analise do procedimento void selecao(int [], int ini, int fim);
    sao necessarias aproximadamente n iterações(comparações) para 
    encontrar o menor elemento em um vetor com n elementos
*/

void odernacaoPorSelecao(int v[], int ini, int fim){
    if(ini >= fim) return;
    //quando mandar vetores como entrada em funcao, n usar colchete vazio, apenas o noome da var basta.
    //n = fim-ini+1;
    //selecao(v, ini, fim);
    //selecao(v, ini+1, fim);
}
